# nodejs---项目相关

在一个文件夹内初始化一个项目  
```
npm init
```

安装依赖  
```
npm install express
```

安装仅开发使用的依赖  
```
npm install --save-dev nodemon
```

运行自定义命令(在package.json中定义)  
```
npm run dev
```

如果要将依赖（名词）的相关信息保存到package.json中，需要加 `--save` 或 `--save-dev` 选项，代表依赖或开发依赖  


---
2020/10/27  
