# 反弹shell检测相关

这里当前只记录Linux下的检测方式。

传统检测方式是匹配进程的命令行、标准输入输出、网络连接信息。

可以用netlink监听进程创建事件，有2个问题：
1. fork和execv事件如果都监听，可能会触发2次
2. 新创建进程在fork事件后execv事件前，命令行等信息是继承自父进程的，不准确
3. 如果只监听execv事件，可能会错过一些反弹shell的创建
